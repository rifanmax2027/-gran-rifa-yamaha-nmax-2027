import crypto from 'crypto';export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Método no permitido'});let{nombre,cedula,whatsapp,email,numeros}=req.body||{};if(!nombre||!cedula||!whatsapp||!email||!Array.isArray(numeros)||numeros.length<3||numeros.length%3)return res.status(400).json({error:'Selecciona 3, 6, 9… números y completa tus datos.'});let b=process.env.SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!b||!k)return res.status(500).json({error:'Falta SUPABASE_SERVICE_ROLE_KEY en Vercel'});let ref='RIFA-'+Date.now()+'-'+crypto.randomBytes(3).toString('hex').toUpperCase();let r=await fetch(`${b}/rest/v1/numeros?numero=in.(${numeros.join(',')})`,{method:'PATCH',headers:{apikey:k,Authorization:`Bearer ${k}`,'Content-Type':'application/json','Prefer':'return=representation'},body:JSON.stringify({estado:'reservado',comprador_nombre:nombre,comprador_cedula:cedula,comprador_whatsapp:whatsapp,comprador_email:email,referencia_pago:ref,estado_pago:'PENDING'})});let d=await r.json();if(!r.ok||d.length!==numeros.length)return res.status(409).json({error:'Uno o más números ya no están disponibles.'});return res.status(200).json({reference:ref,total:numeros.length/3*12000,checkoutUrl:null})}
