export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'Método no permitido'});let b=process.env.SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!b||!k)return res.status(500).json({error:'Falta configuración del servidor'});let r=await fetch(`${b}/rest/v1/numeros_publicos?select=numero,estado&order=numero.asc`,{headers:{apikey:k,Authorization:`Bearer ${k}`}});let d=await r.json();return res.status(r.status).json(d)}
